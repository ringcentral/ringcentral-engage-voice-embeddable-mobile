
doctype html
html
  head
    meta(charset='UTF-8')
    meta(http-equiv='x-ua-compatible' content='IE=edge')
    meta(name='viewport', content='width=device-width, initial-scale=1, shrink-to-fit=no')
    meta(name='referrer', content='always')
    title #{title}
    link(href='//developer.ringcentral.com/assets/images/favicon.ico?v=20181214-0738', type='image/vnd.microsoft.icon', rel='shortcut icon')
  body
    #content-loading(style='text-align:center;padding:30px;font-size:24px') loading...
    script.
      window.rc = !{JSON.stringify(_global)};
      var arr = window.location.href.match(/code=([\w\d-_]+)/);
      window.rc.code = arr ? arr[1] : '';
    script.
      window.localStorage.setItem('rc-authcode', window.rc.code);
      setTimeout(function () {
        window.location.href = window.rc.server
      }, 100)

